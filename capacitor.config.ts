import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "cz.honzikovamoudra.app",
  appName: "Honzíkova moudra",
  webDir: "dist",
  ios: { contentInset: "automatic" },
};
export default config;
